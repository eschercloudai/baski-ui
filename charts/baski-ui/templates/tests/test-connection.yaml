apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "baski-ui.fullname" . }}-test-connection"
  labels:
    {{- include "baski-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "baski-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
